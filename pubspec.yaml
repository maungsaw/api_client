name: apiclient
description: "API Call project."
version: 0.0.1

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio:
  dio_cookie_manager: ^3.4.0
  cookie_jar: ^4.0.9
